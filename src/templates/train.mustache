<div class="content__card">
    <img class="content__avatar" src="{{imageUrl}}">
    <div class="content__text">
        <h1>
            {{heading}}
        </h1> 
        {{&description}}
        
        <span class="content__book">
            {{footer}}
        </span>

    </div>
</div>

<div class="content__pagination">
    <a {{#prevId}}href="./{{prevId}}.html"{{/prevId}}>Предыдущий локомотив</a>
    <a {{#nextId}}href="./{{nextId}}.html"{{/nextId}}>Следующий локомотив</a>
</div>
