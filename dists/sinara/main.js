const t = AOS.init({
    once: true,
});

// var controller = new ScrollMagic.Controller();

// // create a scene
// gsap.to(window, {
//     // scrollTrigger: 'header', // start the animation when ".box" enters the viewport (once)
//     scrollTo: 400
   
// });